{
  "name": "alexandria-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Reconstruction of Alexandria's capture → redact → ALX-1 trace → Lighthouse clone pipeline, with voice notes on tasks.",
  "scripts": {
    "dev": "tsx watch server/index.ts",
    "start": "tsx server/index.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
